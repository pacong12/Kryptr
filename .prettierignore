# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
# vendored third-party agent skills (installed via npx skills)
/.agents/skills/
/.claude/skills/
# generated Prisma client (prisma generate output — never hand-edited)
apps/api/src/generated/
